# Common configuration for both halves.
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_EXT_POWER=y

# RGB is intentionally disabled in this diagnostic build.
# Re-enable it only after PMW3610 movement is confirmed.
