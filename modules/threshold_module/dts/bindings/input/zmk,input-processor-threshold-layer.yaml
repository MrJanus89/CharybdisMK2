description: ZMK pointer movement threshold temporary-layer input processor
compatible: "zmk,input-processor-threshold-layer"

properties:
  "#input-processor-cells":
    type: int
    const: 2

  threshold:
    type: int
    required: true
    description: Accumulated absolute X/Y movement required before enabling the layer

# ZMK's common input-processor binding contributes the first cell name.
# Define only the additional name here so the merged list length remains 2.
input-processor-cells:
  - timeout-ms
